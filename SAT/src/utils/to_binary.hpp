#pragma once

#include <iostream>
#include <string>
using namespace std;

namespace utils {

	// converts the number k into a decimal number
	// stored in a string
	string to_binary(unsigned int k);

} // -- namespace utils

