\newcommand \reporttitle {
    \title{
        \textbf{Box Wrapping Problem} \\
        \hfill \\
        \textsc{Combinatorial Problem Solving} \\
        Course project \\
        \hfill \\
    }
    \author{
        Llu\'is Alemany Puig
    }
    \maketitle
}

\newcommand \centeredcaption[1]
{
    \captionsetup{justification=centering}
    \caption[ ]{#1}
}

\newcommand{\lluis}[1]{\todo[author=\textbf{Lluís},inline,color=Lavender]{#1}}

\newcommand \optlength {\mathcal{L}}
\newcommand \Natural {\mathbb{N}}
\newcommand \Integer {\mathbb{Z}}
\newcommand \Bool {\mathbb{B}}
\newcommand \XTL {x_}

\newcommand \Xtl[1][] {
	\ifthenelse{ \equal{#1}{} }
	{ x_b^{(tl)} }
	{ x_{#1}^{(tl)} }
}
\newcommand \Ytl[1][] {
	\ifthenelse{ \equal{#1}{} }
	{ y_b^{(tl)} }
	{ y_{#1}^{(tl)} }
}
\newcommand \Xbr[1][] {
	\ifthenelse{ \equal{#1}{} }
	{ x_b^{(br)} }
	{ x_{#1}^{(br)} }
}
\newcommand \Ybr[1][] {
	\ifthenelse{ \equal{#1}{} }
	{ y_b^{(br)} }
	{ y_{#1}^{(br)} }
}

\makeatletter
% command \NOINDENT to be used in a group   
\newcommand \NOINDENT{\@@par   % clear parshape parameters
% fool list-awareness code (as supposedly in lstlisting, not checked)
      \@totalleftmargin\z@ \@listdepth\z@ \rightmargin\z@
}
\makeatother

\lstset{language=C++,
	keywordstyle=\color{blue},
	stringstyle=\color{red},
	commentstyle=\color{green},
	morecomment=[l][\color{magenta}]{\#},
	tabsize=4,
	basicstyle=\scriptsize,
	showstringspaces=false
}

