#pragma once

/// C++ includes
#include <utility>
#include <cstdint>
using namespace std;

typedef int length;
typedef int width;
typedef int area;

typedef pair<int, int> corner;
typedef pair<int, int> cell;
